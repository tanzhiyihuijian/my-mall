server:
  port: 821
  servlet:
    context-path: /

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mall_demo?useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull&allowMultiQueries=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root
  jpa:
    database: MYSQL
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    generate-ddl: false
    hibernate:
      ddl-auto: update



logging.level.org.springframework.boot.autoconfigure: ERROR
